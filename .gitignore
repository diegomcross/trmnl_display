# Secrets and local state — never commit these
.env
tokens.json
config.json

# Your private Destiny data dump (send it to Claude directly instead)
snapshot.json

# Generated artifacts
manifest-cache.json
screen.png
screen.bmp
screen.svg
preview-orders-*.png
live-screen.png

# Logs
server.log
watcher.log

# Ad-hoc debug dumps (probe scripts) — not part of the app
debug-cache.json
orders-debug.json
probe-result.json
resolve.json

# Vault Verdict: manifest cache + optional private DIM export
vault-manifest-cache/
dim-data.json

# Node
node_modules/
npm-debug.log*
weapon-watch.json
weapon-tags.json
weapon-watch.json.bak
weapon-tags.json.bak
dim-probe.js
weapon-seen.json
weapon-seen.json.bak
